
import 'package:flutter_bloc/flutter_bloc.dart';

class HomeCubit extends Cubit<void> {
	HomeCubit() : super(null);

	void logout() {
		// Handle the logout process
		// For now, there is no state change to emit
	}
}
